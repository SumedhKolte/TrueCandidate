# Public (anon) credentials only — the dashboard is read-only via RLS.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# Sherlock backend (local uvicorn or your Render URL) — used by the
# "Observe a live meeting" launch form.
VITE_API_URL=http://localhost:8000
